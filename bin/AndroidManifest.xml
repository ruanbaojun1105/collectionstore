<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.arrgetest"
    android:versionCode="1"
    android:versionName="1.2" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/wpstar1"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
        <service android:name=".MusicServer" >
            <intent-filter>
                <action android:name="com.angel.Android.MUSIC" />
                <category android:name="android.intent.category.default" />
            </intent-filter>
        </service>

        <activity
            android:name="example.arrgetest.entArrge"
            android:label="@string/app_name" >
        </activity>
        <activity android:name=".StartView" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".loginSuccess" >
        </activity>
        <activity android:name=".Game" >
        </activity>
        <activity android:name=".arrge" >
        </activity>
        <activity android:name=".loginView" >
        </activity>
        <activity android:name=".firstView" >
        </activity>
    </application>

</manifest>